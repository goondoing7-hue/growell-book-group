# GROWELL 작업 안내

현재 사용자의 요청을 우선한다. 이 저장소는 독서모임 앱이며 상담 업무보드와 별도 프로젝트다.

- 원격 저장소: `goondoing7-hue/growell-book-group`.
- 운영 Vercel 프로젝트: `growell_book_group`, ID `prj_1bSFk5VGEVZnpwmPUPL0ssLcptyA`.
- 운영 주소: `https://growell-book.vercel.app`.
- `index.html`은 앱, `privateCrypto.js`는 개인 기록 v2 암호화·복구 모듈이다.
- `npm.cmd run build`가 소스 검사, 등록된 회귀 테스트, 배포 파일 생성까지 수행한다.
- `npm.cmd run publish -- "변경 설명"`은 지정 저장소의 현재 브랜치에 검증된 허용 경로만 커밋·푸시한다. main은 운영, 다른 브랜치는 Preview다. 원격 변경은 먼저 비교·통합하고 강제 푸시하지 않는다.
- 사용자가 배포를 요청한 변경은 Vercel Ready와 운영 페이지까지 확인한다. 중간 작업 상태를 자동 업로드하지 않는다.
- 새 파일은 `scripts/files.cjs`의 적절한 허용 목록에 추가한다. 생성물·복구 파일·실제 회원 데이터·서버 키를 올리지 않는다.
- 개인 기록의 ID·생성 시각·내용을 보존한다. 잘못된 키, 조회 실패, 복구 실패로 기록을 삭제하거나 빈 값으로 덮어쓰지 않는다.
- 테스트는 임시 데이터로만 수행한다. 회원의 실제 기록이나 운영 비밀번호를 테스트에 사용하지 않는다.
- v33 개인 기록은 v2 형식이다. v2 쓰기 이후 v32로 되돌리면 기록을 읽을 수 없으므로 호환 가능한 버전으로만 롤백한다.
- 서버 함수 원본은 이 저장소에 없다. 프런트엔드 수정만으로 서버 권한·복구 정책을 검증했다고 말하지 않는다.
- 첫 접속과 로그인 후 홈 모두 사용자가 승인한 PC·모바일 '오늘 할 일' 구성이다. `home.css`는 반응형 홈 스타일, `homeDomain.js`는 독서·습관 및 최신 공유 글 선택을 처리한다. 소개와 전체 독서 여정은 `#/about`에서 유지한다. 비로그인 홈에는 개인 진도·습관을 추정해 표시하지 않고 로그인으로 안내한다.
- 홈 공유 글에는 기존 공개 나눔에서 읽을 수 있는 열린 책의 `STATE.posts`만 사용한다. 로그인 시 본인 글을 제외한다. `privateEntries`의 본문이나 복호화 결과를 홈 공유 목록에 전달하지 않는다.

실행·배포와 복구 파일 안내는 `README.md`, `DEPLOYMENT.md`를 따른다.
